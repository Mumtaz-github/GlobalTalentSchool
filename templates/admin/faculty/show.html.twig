{% extends 'base.html.twig' %}

{% block title %}Faculty{% endblock %}

{% block body %}
    <h1>Faculty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ faculty.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ faculty.name }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ faculty.designation }}</td>
            </tr>
            <tr>
                <th>Bio</th>
                <td>{{ faculty.bio }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ faculty.photo }}</td>
            </tr>
            <tr>
                <th>DisplayOrder</th>
                <td>{{ faculty.displayOrder }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_faculty_index') }}">back to list</a>

    <a href="{{ path('app_faculty_edit', {'id': faculty.id}) }}">edit</a>

    {{ include('admin/faculty/_delete_form.html.twig') }}
{% endblock %}
